/* Base */
body {
    background-color: #f7f7f7;
    color: #111;
    font-family: monospace;
    max-width: 860px;
    margin: 40px auto;
    line-height: 1.6;
    padding: 0 20px;
}

/* Links */
a {
    color: #0066ff;
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

/* Navigation spacing */
nav a {
    margin-right: 20px;
}

/* Buttons */
button,
.btn {
    background-color: #f7f7f7;
    color: #0066ff;
    border: 1px solid #0066ff;
    padding: 10px 18px;
    font-family: monospace;
    cursor: pointer;
    transition: all 0.25s ease;
}

/* Hover gradient */
button:hover,
.btn:hover {
    color: white;
    background: linear-gradient(90deg, #0066ff, #4da3ff);
    border-color: #0066ff;
}

/* Simple sections spacing */
section {
    margin-bottom: 50px;
}

/* Footer */
footer {
    margin-top: 80px;
    font-size: 0.9rem;
    opacity: 0.7;
}
